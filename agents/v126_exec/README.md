# V126 control and rejected experimental switches

The packaged default is behaviorally identical to the live V125 policy. Every
new switch is off because none passed the paired adoption gate:

- `ENABLE_EXECUTION_TRANSACTIONS`: failed the four known closed-loop cases.
- `ENABLE_GENERAL_JOB_RECOVERY`: broader E prototype, also rejected.
- `ENABLE_SERVICE_POLICY`: lost 6 of 16 paired development games versus control.
- `ENABLE_FERTILIZER_POLICY`: lost 2 of 16 and reduced every paired margin.
- `ENABLE_ANIMAL_PURCHASE_GUARD`: remains rejected from the broad first pass.

The dormant code and actor-local tests are retained as falsified development
evidence; they are not claims that the execution bug was fixed. The rejected
V125 base/adaptive farm-plan experiments remain separate and disabled.
